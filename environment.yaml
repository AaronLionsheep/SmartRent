name: indigo-smartrent-dev
channels:
  - defaults
  - conda-forge
dependencies:
  - python ~=3.13.0
  - ruff
  - pytest
  - pip
  - pip:
    - -r SmartRent.indigoPlugin/Contents/Server\ Plugin/requirements.txt